[Service]
ExecStartPre=
ExecStartPre=+bash -c 'if ! id clickhouse &>/dev/null; then groupadd -r clickhouse && useradd -r -g clickhouse -d /var/lib/clickhouse -s /sbin/nologin clickhouse && mkdir -p /run/clickhouse && chown clickhouse:clickhouse /run/clickhouse /var/lib/clickhouse; fi'
